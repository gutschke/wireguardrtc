wireguardrtc (0.2.9-1) unstable; urgency=medium

  * Android: RoamController is now wired into the joiner-N path —
    each active joiner re-races candidates in place when the
    phone's underlying network changes (Wi-Fi to cellular and
    similar). Previously only the legacy single-joiner path got
    automatic re-race; joiner-N relied on the daemon volunteering
    a fresh OFFER and could sit on a dead endpoint until then.
  * Test infrastructure: `HostCatchallForwarderE2ETest.udpThroughCatchallRoundTrips`
    UDP echo no longer flakes ~1-in-4 (now 20/20 across 10
    sequential runs on both emulator and ARC). Two fixes: a
    500 ms post-handshake settle before the first send (gvisor
    netstack route-table propagation race), and proper retransmit
    semantics for UDP (every 200 ms for up to 15 s).
  * Instrumented test rig: new `VpnConsentHelper` dismisses the
    Android 14+ VPN consent dialog via UI Automator so
    `JoinerNVpnServiceInstrumentedTest.twoJoinersCoexistOnSharedStack`
    runs end-to-end against real Builder.establish() instead of
    skipping.
  * Adversarial stress: 13-scenario JVM stress suite for
    `JoinerNController` covering concurrent add, mid-rebuild
    reconfigure, mutex correctness, failure injection, and
    use-after-close detection.

 -- wireguardrtc maintainers <maintainers@wgrtc.invalid>  Fri, 16 May 2026 04:30:00 +0000

wireguardrtc (0.2.8-1) unstable; urgency=medium

  * Android: opt-in multi-joiner shared-tunnel path.  Settings →
    Experimental → "Multi-joiner shared tunnel" lets the app keep
    more than one joiner-mode tunnel up at the same time by sharing
    one Android VPN slot across all of them (one userspace gvisor
    netstack, N wireguard-go bridges).  Default off — the legacy
    single-joiner path is unchanged when the toggle is off.  Real-
    device validation: emulator (API 34) + ARC ChromeOS, settings
    persistence + on/off cycle.  End-to-end two-joiner handshake
    needs VPN consent and is the next instrumented test step.
  * Code review pass on the joiner-N ViewModel path caught three
    concurrent-access bugs: shared connectJob scalar (fixed by a
    per-tunnel-id Map), maybeUnbindJoinerN dropping the binding
    under a racing sibling connect (fixed by an in-flight counter),
    and an unrelated-tunnel NetworkChangeMonitor.stop().
  * Documentation: US English spelling sweep (53 files) — joiner-
    side enrolment/behaviour/colour/serialise consistently use the
    American forms.  Codename pruning in production comments (24
    files, 51 references) — internal D4.*/V6.*/PS* task tags
    removed from places where they no longer carried signal.
    User-facing docs (v6-runbook.md, cascade-n-design.md) lost
    their internal task-stream framing.

 -- wireguardrtc maintainers <maintainers@wgrtc.invalid>  Thu, 15 May 2026 16:00:00 +0000

wireguardrtc (0.2.7-1) unstable; urgency=medium

  * Android: host-N landed.  HostModeBackend now supports N concurrent
    host tunnels per device via a slot map keyed by tunnel id.  Per
    tunnel: independent peer set, listen-port collision guard, isolated
    stats.  ViewModel + UI surface every tunnel's state through a
    unified flow.  FGS notification expands to enumerate the active
    set when more than one host is up.
  * Android: joiner-N foundation (Go-side + Kotlin controller).  New
    `wgbridge_native` shared-stack architecture lets one VpnService
    TUN carry N joiner-mode wireguard-go bridges, demuxed by gvisor's
    longest-prefix-match.  Not yet wired into the UI — the new
    `JoinerNVpnService` runs in parallel to the legacy
    `JoinerVpnService` pending an opt-in flag (planned for v0.2.8).
  * No daemon-side changes in this release.

 -- Markus Gutschke <markus@claude.gutschke.com>  Fri, 15 May 2026 12:00:00 -0800

wireguardrtc (0.2.6-1) unstable; urgency=medium

  * V6 series: dual-stack IPv6 support, end-to-end.  Daemon (V6.D1,
    V6.D3, V6.D4, V6.D5): production stun_query takes a `family=`
    kwarg, candidate enumerator emits comma-separated dual-stack
    `PublicIp` lists, local interface enumeration walks v6 addresses,
    hole-punching is skipped for v6 endpoints (raw-IP injection is
    IPv4-only and v6 doesn't typically need NAT traversal).
  * V6 series joiner (V6.A1, V6.A2, V6.A3): happy-eyeballs prefers
    v6 within each candidate-kind tier; VpnService accepts `::/0`
    routes; MTU defaults pick 1400 (v6 outer) vs 1420 (v4 outer)
    based on the endpoint family.
  * V6 series host-mode (V6.H1, V6.H2, V6.H2b, V6.H3): gvisor
    netstack accepts dual-stack address lists; catchall TCP/UDP
    forwarders bracket v6 destinations; new ICMPv6 echo through-host
    forwarder with RFC 4443 checksum and gvisor temp-local-address
    re-injection (validated on real ChromeOS hardware, 7 ms
    round-trip to Google v6 DNS); DnsProxy v6 wire-format pinned.
  * V6.2 + V6.3: each host tunnel mints a per-tunnel ULA at creation
    (`HostSubnetAllocator.generateUlaPrefix`); peer enrolment
    allocates v4 + v6 simultaneously; enrolment payload carries
    comma-separated dual-stack `address` and `AllowedIPs`,
    whitespace-tolerant on input, canonical (no whitespace) on
    output for ChromeOS compatibility.
  * Whitespace canonicalisation: every place that emits an
    `AllowedIPs` line strips spaces (PS18 follow-up).
  * Documentation: design doc status flipped to "Shipped in v0.2.6",
    V6 runbook added (`docs/v6-runbook.md`), top-level + Android
    READMEs mention dual-stack.

 -- Markus Gutschke <gutschke@users.noreply.github.com>  Thu, 15 May 2026 14:00:00 -0700

wireguardrtc (0.2.5-1) unstable; urgency=medium

  * D1.10 follow-ups: make a vanilla-Ubuntu install actually work for
    daemon-side wormhole pairing without manual config rescue.
  * F1 (broker UX): drop the literal `your.broker.example` placeholder
    from the shipped config.  Daemon now refuses to start until the
    admin picks a broker — inline error message names both viable
    options (public 0.peerjs.com vs self-host Docker recipe) so the
    admin doesn't need to consult external docs.
  * F2 (enrollment defaults): ship `[Enrollment]` UNCOMMENTED so the
    out-of-the-box install supports wormhole + token enrollment via
    the broker-mediated default ProvisionScript path.
  * F3 (daemon-managed pairs): `--mint-wormhole` and `--use-wormhole`
    now write a peers.d-style drop-in to
    `/var/lib/wireguardrtc/auto-enrolled.d/` after a successful pair.
    The daemon picks it up and actively maintains the new tunnel.
    Drop-in files are chowned to the daemon user when written as
    root.  Daemon logs a one-line kernel-vs-managed-peer inventory
    after the first poll so admins notice peers that aren't being
    maintained.
  * F4 + F5 (provision-default): the address allocator was silently
    broken by a bash heredoc/herestring redirection quirk (always
    returned pool.hosts()[0]).  Rewrite to pass USED via argv;
    [Interface] Address is now properly reserved.  New
    `WIREGUARDRTC_PROVISION_ASSIGNED_ADDRESS` env var lets the
    wormhole-pair host advertise a specific address to the joiner
    and have provision-default honour it (validating pool membership
    + collision).

 -- Markus Gutschke <gutschke@users.noreply.github.com>  Wed, 14 May 2026 13:25:00 -0700

wireguardrtc (0.2.4-1) unstable; urgency=medium

  * Wormhole pairing for the daemon, parity with the Android app
    (D1).  Two new subcommands:
      --mint-wormhole [--label NAME] [--iface IFACE] [--parameters STR]
        prints a 6-letter wormhole code + waits for a peer to dial in;
      --use-wormhole CODE [--iface IFACE]
        types the 6-letter code on a remote machine and joins the host.
    Both sides verify the SAS phrase before committing the new peer.
    Ristretto255 / SPAKE2 / SAS implementation is vendored — no pypi
    dependency for the cryptography.
  * --check-nat subcommand: independent IPv4 / IPv6 classification
    (full-cone / address-restricted / port-restricted / symmetric /
    none-detected) with optional rDNS + ISP enrichment.
  * ProvisionScript hook: wormhole-pair flow now invokes the same
    hook as token-based enrollment, passing the admin-supplied
    parameter string via the new WIREGUARDRTC_PROVISION_PARAMETERS
    environment variable.  Backward-compatible: scripts that read the
    sidecar still work unchanged.
  * Test-only command-line flags (--pubkey-override, --auto-confirm-sas)
    are now gated behind WIREGUARDRTC_ALLOW_TEST_FLAGS=1.  Without
    the gate they're hidden from --help and refuse to run.
  * Documentation: man pages are mirrored as docs/*.md (auto-generated
    from troff).  New docs/provision-scripts.md describes how to
    write a custom ProvisionScript.

 -- Markus Gutschke <gutschke@users.noreply.github.com>  Wed, 13 May 2026 20:44:00 -0700

wireguardrtc (0.2.3-1) unstable; urgency=high

  * Release blocker: restore daemon from v0.2.0 corruption.  A
    code-cleanup pass in the v0.2.0 restructure stripped ~80
    trailing `()` from function calls plus three test files'
    indentation; the file remained syntax-valid because each
    corrupted site is a legal bound-method reference (e.g. `main`
    instead of `main()` at module bottom).  Every fresh-install of
    v0.2.0's .deb produced a daemon that exited 0 silently and did
    nothing.  Existing v0.1.0 installs kept working only because
    they were still running the old binary; v0.1.0 -> v0.2.0
    upgrades silently broke.
  * Daemon repair touched ~96 call sites across `wireguardrtc`,
    three test files (`25_parallel_stun.py`, `26_candidate_trim.py`,
    `27_candidate_enumerate.py`), and `fabric.sh`'s nine function
    definitions.  Restored `get_public_ipv4_all`'s missing
    `LOG.event` for the PublicIp override (test 09 caught it).
  * Two CI safeguards added: `tests/28_help_smoke.py` (runs
    `wireguardrtc --help` and fails on empty stdout) and
    `tests/29_ast_lint.py` (flags bare Name/Attribute expressions,
    method refs in boolean contexts, and method-shaped attr
    assignments).
  * Full test pass: 175 daemon assertions + 11 fabric end-to-end
    PASS across the unit and shell/fabric suites.

 -- Markus Gutschke <gutschke@users.noreply.github.com>  Wed, 13 May 2026 12:00:00 +0000

wireguardrtc (0.2.0-1) unstable; urgency=low

  * Repository restructured to host the daemon and the Android client
    side by side on a unified version tag.  The daemon source moves
    from the repo root to `daemon/`; behaviour is unchanged.
  * Relicensed from MIT to Apache License 2.0 (the daemon's source
    has no GPL or otherwise-incompatible dependencies; Apache 2.0 was
    chosen for its explicit patent grant).
  * Repo renamed from `wireguardrtc` to `wgrtc` to match the Android
    package id and avoid confusion with the trademarked WireGuard name.

 -- Markus Gutschke <gutschke@users.noreply.github.com>  Mon, 12 May 2026 00:00:00 +0000

wireguardrtc (0.1.0-1) unstable; urgency=low

  * Initial release.

 -- Markus Gutschke <gutschke@users.noreply.github.com>  Sun, 03 May 2026 00:00:00 +0000
