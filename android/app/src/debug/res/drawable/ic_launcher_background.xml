<!--
  debug buildType launcher background — amber tint so the debug
  install is visually distinct from the release variant when both
  are present on a device.  Same shape, same teal hexagon
  foreground; only the background colour changes.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFD180"
        android:pathData="M0,0h108v108h-108z" />
</vector>
