<!--
  agent buildType launcher background — pink tint so the
  instrumented test rig is visually distinct from release and
  debug variants when all three are present on a device.  Pink
  was chosen to be unambiguously different from the release
  teal and the debug amber, and to subtly signal "test only".
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#F8BBD0"
        android:pathData="M0,0h108v108h-108z" />
</vector>
