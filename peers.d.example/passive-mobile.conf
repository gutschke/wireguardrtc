# Example: a phone or laptop that's online intermittently. The daemon
# never reaches out to the broker for this peer (would burn the broker's
# connection limit chasing a sleeping device), but it WILL accept an
# incoming OFFER if the device dials in itself.

[Peer]
PublicKey = REPLACE_WITH_BASE64_PUBLIC_KEY_OF_PEER
Mode = passive
